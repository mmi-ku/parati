<h1>Users List</h1>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= user.name %> - <%= user.email %>
      <%= link_to 'Delete', admin_user_path(user), method: :delete, data: { confirm: 'Are you sure you want to detele？' } %> 
    </li>
  <% end %>
</ul>