

<h2>Profile Edit</h2>
<%= form_with model: @user do |f| %>
  <label for="inputName">Name</label>
  <%= f.text_field :name, autofocus: true, id:"inputName", placeholder:"name"%>
  <label for="inputImage">ProfileImage</label>
  <%= f.file_field :image, placeholder: "Profile Picture", accept: "image/*" %>
  <%= f.submit "Save" %>
<% end %>

  <%= link_to 'Delete My Account', withdraw_user_path(@user), method: :patch, data: {confirm: "Are you sure you want to ？"} %>
